## What this changes

<!-- One or two sentences. Which workstream, which directory. -->

## Checklist

- [ ] `pnpm verify` passes locally
- [ ] Nothing logs a *value* — ids, counts and durations only
- [ ] No site-specific selectors (`.class-name`, `#id`) in the logic
- [ ] If I changed `packages/schema`, I said so in the title — it is a contract
      four other people build against
